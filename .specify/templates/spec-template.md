# 规格 Spec：<功能名>

- **模块**：<user | activity | social | team | chat | admin>
- **负责人**：<名字>
- **关联需求**：需求文档第 X 节《...》；`docs/requirements-breakdown.md` 中的条目编号
- **分支**：`feat/<module>-<slug>`

## 1. 目标（一句话）
这个功能让 <谁> 能够 <做什么>，从而 <价值>。

## 2. 验收标准（可测试，逐条）
- [ ] 给定 <前提>，当 <操作>，则 <可观察结果>。
- [ ] 边界/异常：<名额满 / 截止 / 越权 / 重复操作 …>
- [ ] 非功能：<相关接口 < 2000ms / 列表分页 …>

## 3. 涉及的契约（必须先确认/补充 contracts/）
- **端点**：`GET /v1/...`、`POST /v1/...`（请求/响应 schema 名）
- **DB 表/列**：<表名 + 关键字段>
- **枚举 / 错误码**：<新增项，需登记到 enums.md / error-codes.md>
- 若需改契约：ADR 编号 = `docs/adr/XXXX-...md`

## 4. 范围
- **做**：...
- **不做（Out of scope）**：...（明确列出，避免 AI 自行扩张）

## 5. UI / 交互（前端）
- 页面 / 组件：`frontend/src/views/<module>/...`
- 关键交互、状态、空/错/加载态

## 6. 端到端验证步骤
描述一条"证明功能可用"的路径（人能照着点一遍/脚本能跑一遍）。
